<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Prediction Results</title>
    <link rel="stylesheet" href="styles.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f9;
        }
        h1 {
            text-align: center;
            color: #333;
            margin-top: 20px;
            font-size: 24px;
            text-underline-offset: 1;
        }
        table {
            width: 100%;
            max-width: 800px;
            margin: 20px auto;
            border-collapse: collapse;
        }
        th, td {
            padding: 12px;
            text-align: left;
            border: 1px solid #ddd;
            font-weight: 500;
        }
        th {
            background-color: #f2f2f2;
        }
        button {
            display: block;
            width: 150px;
            padding: 10px;
            margin: 20px auto;
            border: none;
            border-radius: 5px;
            background-color: #007bff;
            color: #fff;
            font-size: 14px;
            cursor: pointer;
        }
        button:hover {
            background-color: #0056b3;
        }
        .error {
            color: red;
            text-align: center;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <h1><u>PREDICTED ENTITIES AND SCORE</u></h1>

    <% if (error) { %>
        <div class="error"><%= error %></div>
    <% } else { %>
        <h4 style="text-align: center;">
            <span style="color: blue;">Product description for </span>
            <span style="color: green;"><%= userInput %></span>
          </h4>
        <% if (results && results.length > 0) { %>
            <table>
                <thead>
                    <tr>
                        <th>HTS Code</th>
                        <th>Description</th>
                        <th>Score</th>
                        <th>Tarif</th>
                        <th>Measurement Unit</th>
                    </tr>
                </thead>
                <tbody>
                    <% results.forEach(result => { %>
                        <tr>
                            <td><%= result.htsNumber %></td>
                            <td><%= result.description %></td>
                            <td><%= result.score %></td>
                            <td><%= result.tarif %></td>
                            <td><%= result.measurementUnit %></td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } else { %>
            <p>No matching results found.</p>
        <% } %>
    <% } %>
    <button onclick="window.location.href='/Frontend/index.html'">Go Back</button>
</body>
</html>
